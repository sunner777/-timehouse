# 下阶段开发计划

> 最后更新：2026-06-27（基于服务器最新代码审计）

## 已完成 ✅

### V2.0 核心功能（全部完成）
| 模块 | 内容 | 状态 |
|------|------|------|
| 认证 | SMS 验证码登录/注册，密码登录（已废弃），JWT+Redis 黑名单，修改密码 | ✅ |
| 照片 | 上传（TOS 预签名直传）、浏览、大图、删除、批量删除、SHA-256 去重 | ✅ |
| 家庭组 | CRUD、手机号添加成员、RBAC 四级权限、跨用户照片访问 | ✅ |
| UI | 3-Tab 导航、渐进式懒加载网格、SQLite 缓存、零转圈加载、错误态+重试 | ✅ |
| App 发布 | 签名证书、隐私政策/用户协议页面、ICP 备案号 | ✅ |

### 基础设施
| 模块 | 内容 | 状态 |
|------|------|------|
| 服务器 | 阿里云 ECS 99 计划，`api.timehouse.top`，Docker MySQL+Redis | ✅ |
| 存储 | 火山引擎 TOS 私有桶，预签名 URL 访问 | ✅ |
| 安全 | helmet + Joi 校验 + sanitize + rate-limit + 审计日志 + bcrypt + HSTS | ✅ |
| 代码 | GitHub `sunner777/-timehouse`，Git 部署管道 | ✅ |

---

## 待修复 🐛

### Bug #1：Flutter 调用不存在的后端接口 🔴

| Flutter 方法 | 调用的 API | 后端状态 |
|------|------|------|
| `FamilyProvider.joinFamily()` | `POST /families/join` | ❌ 不存在 |
| `FamilyProvider.generateInviteCode()` | `POST /families/:familyId/invite` | ❌ 不存在 |

用户已明确不需要邀请码功能。两个方案：
- **方案 A（推荐）**：从 Flutter 端移除这两个方法及相关 UI（`ApiService.joinFamily/generateInviteCode`、`FamilyProvider` 中的调用、`families_screen.dart` 中的邀请入口）
- **方案 B**：补上后端接口

- [ ] 清理 Flutter 端邀请码相关死代码

### Bug #2：`photos_screen.dart` 是死代码 🟡

该文件不被 `app_router.dart` 引用，且使用旧的直传上传模式（非 TOS 流程），属于 V1.0 残留。

- [ ] 删除或归档 `photos_screen.dart`

---

## 待优化 🟢

| 项目 | 现状 | 方案 |
|------|------|------|
| hash 唯一索引 | 去重通过 SELECT 查询实现，正常工作；唯一索引可为并发竞态加保险 | `CREATE UNIQUE INDEX ON photos(hash)` |
| 服务端缩略图 | `url` 和 `thumbnailUrl` 存同一张原图，网格列表浪费带宽 | sharp 生成缩略图上传到 TOS |
| CORS 生产限制 | `CORS_ORIGIN=*` 允许任意来源 | 改为仅允许 App 来源 |
| PM2 单实例 | fork 单进程，崩溃需手动恢复 | 改 cluster 2 实例 |
| 空测试目录 | `backend/tests/` 存在但无测试文件 | 补充核心 API 测试 |
| 空目录残留 | `src/models/mongodb/`、`src/queues/`、`src/workers/` 空目录 | 清理 |
| 认证审计日志 | login/logout/register 缺少 auditLog 调用 | 补充 |

---

## 待开发 🔵

### V3.0 人脸识别（调研阶段）

- 技术选型：阿里云视觉智能 / 百度 AI / 本地模型
- 人脸聚类 + 按人物浏览照片
- 隐私合规评估

---

## 用户侧待办

- [ ] 应用截图（各尺寸）
- [ ] 软著申请
- [ ] 华为/小米/OPPO/vivo 应用商店提交

---

## 不在计划中

| 项目 | 原因 |
|------|------|
| 邀请码加入/重新生成 | 已砍掉，手机号直接添加替代 |
| 短信邀请成员 | 同上 |
| 动效过渡/动画 | 不需要 |
| App 内推送通知 | 当前用户规模不需要 |
| MongoDB | V1.0 起未使用 |
| CDN | 当前带宽够用 |
