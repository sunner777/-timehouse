# 时光家项目开发日志

## 2026-05-16

### 今日进展
- ✅ 完成 V2.0 UI 架构重构：3-Tab 底部导航（共享组 | 我的空间 | 我的）
- ✅ 创建「我的空间」页面，双子 Tab（照片 + 共享组），支持批量管理
- ✅ 创建全局 UploadFAB 组件，提供统一的上传 → 选可见范围流程
- ✅ 创建可复用 PhotoGridView 组件，支持日期分组和选择模式
- ✅ 修复 4 个共享组浏览 bug
- ✅ 后端新增跨用户照片访问支持
- ✅ 用户验证通过，V2.0 核心流程闭环

### 架构重构

#### 前端新增/重写文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `widgets/upload_fab.dart` | 新增 | 全局上传 FAB 组件，完整流程：选图→上传TOS→保存元数据→选共享组可见范围→更新归属 |
| `widgets/photo_grid_view.dart` | 新增 | 可复用照片网格，支持日期分组、3列布局、多选模式 |
| `screens/my_space_screen.dart` | 新增 | Tab 2 页面，双子 Tab「照片」\|「共享组」，长按多选→批量删除/改可见范围 |
| `screens/families_screen.dart` | 重写 | Tab 1 页面，顶部横向 ChoiceChip 切换组，照片网格 + UploadFAB |
| `screens/photo_detail_screen.dart` | 重写 | 改为 StatefulWidget，本地未命中时从 API 加载；共享组上下文显示「从组中移除」 |
| `screens/family_members_screen.dart` | 重写 | showModalBottomSheet 替换 Scaffold.bottomSheet，修复添加成员按钮无效 |
| `widgets/bottom_nav_bar.dart` | 修改 | 新 3-Tab：共享组 \| 我的空间 \| 我的 |
| `routes/app_router.dart` | 修改 | / → FamiliesScreen，新增 /my-space → MySpaceScreen，/photo/:id 支持 familyId extra |
| `providers/photo_provider.dart` | 修改 | uploadPhoto 返回 photoId；新增 getApiService()、getPhoto()、getFamilyPhotosCached()、invalidateFamilyPhotos() |
| `services/api_service.dart` | 修改 | 新增 updatePhotoFamily()、getPhoto() |

#### 后端修改
| 文件 | 操作 | 说明 |
|------|------|------|
| `models/mysql/Photo.js` | 修改 | 新增 findByIdOnly(id)，不按 userId 过滤 |
| `services/photoService.js` | 修改 | getPhotoDetail 支持跨用户访问：先 userId 查 → 未命中则 findByIdOnly → 验证 family 成员身份 |

### Bug 修复记录

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 共享组删除照片是彻底删除 | PhotoDetailScreen 不区分上下文 | 传入 familyId，共享组内显示「从组中移除」，调用 updatePhotoFamily(id, null) |
| 2 | 大图回退跳到共享组 Tab | GoRouter.go('/') 替换了路由栈 | 移除显式导航，依赖 AppBar 默认 context.pop() |
| 3 | 相机 FAB 只在旧页面有 | 未抽离为全局组件 | 创建 UploadFAB，集成到 families_screen 和 my_space_screen |
| 4 | 共享组移除照片后不立即刷新 | families_screen 本地缓存未失效 | 改用 await context.push，返回后 invalidateFamilyPhotos + setState |
| 5 | 登录后共享组直接看大图不显示 | ① 后端 Photo.findById 按 userId 过滤，跨用户照片 404；② 前端 photoProvider.photos 未初始化 | ① 后端新增 findByIdOnly + 成员身份校验；② 前端 PhotoDetailScreen 改为 StatefulWidget，API 兜底加载 |
| 6 | 共享组上传照片后不立即显示 | 缓存存在 families_screen 本地，UploadFAB 无法触达 | 缓存提升到 PhotoProvider（getFamilyPhotosCached + invalidateFamilyPhotos），families_screen 监听 PhotoProvider |

### 开发经验总结

1. **showModalBottomSheet 而非 Scaffold.bottomSheet** — 后者设 null 不会自动关闭已打开的 Sheet，导致 UI 无响应。对话框类交互统一用 showModalBottomSheet，通过 Navigator.pop(value) 返回结果。

2. **context.push() 而非 context.go()** — go() 替换整个路由栈，破坏底部导航选中状态；push() 压栈，pop() 自然回到上一页，导航状态保持一致。

3. **跨组件共享缓存放在 Provider 里** — 多个 Widget 用到同一份数据时，缓存放在 Provider 而非某个 Widget 的本地 state。配合 notifyListeners + context.watch，数据变更自动传播到所有监听者。

4. **共享组照片访问需后端放开 userId 过滤** — Photo.findById 加了 userId 条件后，其他成员上传的照片查不到。需要 findByIdOnly + family 成员身份校验两步走。

5. **await context.push() 模式** — 从 A 页面 push 到 B，B 操作完 pop 回来，A 用 await 等待 push 完成，然后在 then 里刷新数据。比传递回调更简洁。

6. **API 兜底加载** — 不要假设 photoProvider.photos 总是已填充。大图浏览页作为独立路由，可能在列表页请求之前就进入，应支持从 API 按 id 单独加载。

7. **后端响应格式要保持一致** — getPhotos 返回 `{ photos: [...] }`，getPhotoDetail 返回直接是 photo 对象。前端解析时用 `data['photo'] ?? data` 兼容两种格式。

8. **FutureBuilder 的 future 要稳定** — 每次 build 创建新 Future 会导致无限循环。解决方式：用带缓存的 Provider 方法，缓存命中时同步返回，FutureBuilder 看到已完成 Future 不重复请求。

---

## 2026-05-10

### 今日进展
- ✅ 完成家庭组共享功能后端开发
- ✅ 创建Family和FamilyMember模型
- ✅ 实现家庭组服务和控制器
- ✅ 配置家庭组相关路由
- ✅ 更新照片管理支持家庭组
- ✅ 完成数据库迁移（添加family_id字段）
- ✅ 创建API测试脚本
- ✅ 完成前端家庭组功能开发
- ✅ 创建共享组页面（Tab页面）
- ✅ 创建成员管理页面
- ✅ 配置家庭组路由和底部导航
- ✅ 前后端联调测试
- 🔄 家庭组核心功能测试中（发现问题并修复）

### 测试进展记录

#### 一、基础环境检查 ✅【已完成】
- ✅ 后端服务启动正常
- ✅ Flutter编译成功，APP正常启动
- ✅ 数据库连接成功

#### 二、用户认证测试 ✅【已完成】
- ✅ 用户注册功能正常
- ✅ 用户登录功能正常
- ⚠️ 发现问题：密码错误时返回400而非401
- ✅ 已修复：将认证相关错误码（1003/1004/1005）映射为HTTP 401状态码

#### 三、家庭组核心功能测试 🔄【进行中】
- ✅ 点击「共享组」Tab页面，页面正常显示
- ⚠️ 发现问题1：点击「创建组」后页面红屏崩溃
  - 原因：FamilyProvider未在main.dart中注册
  - 修复：添加FamilyProvider到MultiProvider
- ⚠️ 发现问题2：点击「创建组」无法弹出输入框
  - 原因：按钮点击后只设置了状态变量，未实现对话框显示逻辑
  - 修复：实现_showCreateFamilyDialog和_showJoinFamilyDialog方法，使用showModalBottomSheet
- ⚠️ 发现问题3：输入组名点击确认后，前端一直显示加载中，后端日志出现大量重复请求
  - 原因：FutureBuilder在每次Widget构建时都会重新执行future，导致getFamilyPhotos被重复调用，引发无限循环
  - 修复：添加缓存机制（_familyPhotosCache和_familyPhotosLoading），使用setState替代FutureBuilder
- 当前状态：已完成初步修复，等待用户验证

### 详细记录
1. **数据库模型**
   - 创建Family模型（models/mysql/Family.js）
   - 创建FamilyMember模型（models/mysql/FamilyMember.js）
   - 为Photo模型添加familyId支持

2. **后端服务层**
   - 创建familyService.js，实现家庭组业务逻辑
     - 创建家庭组（自动生成邀请码）
     - 获取家庭组列表和详情
     - 邀请码机制（生成/重新生成）
     - 成员管理（加入/添加/移除/权限更新）
     - 退出家庭组
   - 更新photoService.js，支持家庭组照片查询

3. **API接口层**
   - 创建familyController.js控制器
   - 更新photoController.js，增加家庭组照片支持
   - 新增邀请码生成工具（utils/codeGenerator.js）

4. **路由配置**
   - 创建routes/family.js路由文件
   - 更新routes/index.js引入家庭组路由
   - 更新routes/photo.js增加家庭组归属更新接口

5. **数据库迁移**
   - 创建migrate-add-family-id.js迁移脚本
   - 为photos表添加family_id字段和外键约束

6. **前端开发**
   - 创建family.dart模型
   - 创建family_member.dart模型
   - 创建family_provider.dart状态管理
   - 创建families_screen.dart共享组页面
   - 创建family_members_screen.dart成员管理页面
   - 更新api_service.dart添加家庭组API方法
   - 更新app_router.dart添加家庭组路由
   - 更新bottom_nav_bar.dart添加共享组Tab
   - 更新main.dart注册FamilyProvider

7. **BUG修复**
   - 修复认证错误码问题：密码错误返回400→改为返回401
   - 修复Provider未注册问题：FamilyProvider添加到MultiProvider
   - 修复对话框未实现问题：使用showModalBottomSheet实现
   - 修复FutureBuilder无限循环问题：添加缓存机制

8. **新增API接口列表**
   | 方法 | 路径 | 功能 |
   |------|------|------|
   | POST | /api/v1/families | 创建家庭组 |
   | GET | /api/v1/families | 获取家庭组列表 |
   | GET | /api/v1/families/:familyId | 获取家庭组详情 |
   | GET | /api/v1/families/:familyId/photos | 获取家庭组照片 |
   | POST | /api/v1/families/:familyId/invite | 重新生成邀请码 |
   | POST | /api/v1/families/join | 使用邀请码加入 |
   | POST | /api/v1/families/:familyId/members | 添加成员 |
   | PUT | /api/v1/families/:familyId/members/:userId/permissions | 更新成员权限 |
   | DELETE | /api/v1/families/:familyId/members/:userId | 移除成员 |
   | POST | /api/v1/families/:familyId/leave | 退出家庭组 |
   | PUT | /api/v1/photos/:id/family | 更新照片的家庭组归属 |

### 技术配置更新
- 后端服务端口：3000
- MySQL端口：23306
- MySQL密码：112358
- TOS桶名：timehouse-photos-cn-shanghai
- TOS区域：cn-shanghai（华东2）
- TOS Endpoint：tos-cn-shanghai.volces.com

### 今日教训（重要）
1. **正确使用SDK导入方式**：TOS SDK新版必须使用解构导入`const { TosClient } = require('@volcengine/tos-sdk')`，而非旧版`const TOS = require('@volcengine/tos-sdk')`
2. **技术方案必须考虑中国大陆用户**：所有服务选择国内服务商，避免使用可能被网络限制的服务
3. **多次修改无效时寻求帮助**：如果遇到相同错误多次修改无效，需要思考是否有沙箱环境限制，及时向用户说明情况
4. **Provider必须正确注册**：使用Provider时必须在main.dart的MultiProvider中注册，否则会导致红屏崩溃
5. **FutureBuilder慎用**：FutureBuilder在每次Widget构建时会重新执行future，可能导致无限循环，必要时使用缓存机制

### 明日计划
1. 继续家庭组核心功能测试验证
2. 完成成员管理页面测试
3. 完成照片共享功能测试
4. 完成权限验证测试

### 开发状态概览
- **后端API开发**：已完成（认证、照片管理、家庭组API）
- **数据库设计**：已完成
- **TOS云存储集成**：已完成
- **前后端联调**：已完成
- **前端家庭组页面**：已完成
- **家庭组功能测试**：进行中
- **V2.0家庭组功能**：后端完成，前端完成大部分，测试中

---

## 2026-04-26

### 今日进展
- ✅ 完成Docker数据库容器的配置和连接
- ✅ 后端服务基础架构搭建
- ✅ 数据库初始化完成
- ✅ 认证系统代码框架搭建
- ✅ 照片管理API实现
- ✅ 前后端联调配置完成
- ✅ 火山引擎TOS云存储集成
- ✅ 所有核心功能测试通过

### 详细记录
1. **数据库容器配置**
   - 成功配置MySQL容器，端口映射为23306:3306
   - 配置MongoDB和Redis容器
   - 确认所有数据库服务正常运行

2. **后端服务搭建**
   - 完成Node.js Express项目初始化
   - 配置环境变量文件(.env)
   - 实现数据库连接配置
   - 启动服务成功，健康检查接口正常

3. **数据库初始化**
   - 创建数据库初始化脚本(scripts/initDb.js)
   - 设计并创建用户表、家庭组表、家庭组成员表、照片表
   - 成功执行数据库初始化，创建所有数据表

4. **认证系统框架**
   - 实现User模型(mysql/User.js)
   - 实现认证服务(authService.js)
   - 实现认证控制器(authController.js)
   - 实现JWT认证中间件(auth.js)
   - 配置认证相关路由

5. **照片管理API**
   - 实现Photo模型(mysql/Photo.js)
   - 实现照片服务(photoService.js)
   - 实现照片控制器(photoController.js)
   - 配置照片管理相关路由

6. **前后端联调配置**
   - 更新前端API服务配置，将baseUrl改为http://10.0.2.2:3000/api/v1（Android模拟器访问）
   - 实现真实的API调用，替换mock数据
   - 更新UserProvider和PhotoProvider，适配新的API响应格式
   - 确保token管理和认证流程正常

7. **火山引擎TOS云存储集成**
   - 添加@volcengine/tos-sdk依赖
   - 创建tosService.js，实现TOS客户端初始化
   - 配置.env中的TOS参数（AK/SK/Region/Endpoint/Bucket）
   - 实现TOS上传签名API（POST /api/v1/photos/tos-upload-signature）
   - 修改getPreSignedUrl方法正确调用TOS SDK
   - 实现下载预签名URL生成，适配私有桶访问
   - 前端实现真实TOS上传流程（获取签名→直接上传到TOS）
   - 后端在返回照片列表时自动生成下载预签名URL

8. **BUG修复**
   - 修复URL格式问题（移除反引号和多余空格）
   - 修复上传后图片不刷新问题（上传完成后重新获取照片列表）
   - 修复私有桶访问问题（使用下载预签名URL）
   - 修复err.code.startsWith错误处理问题

### 今日教训（重要）
1. **禁止使用国外占位图服务**：选择了via.placeholder.com等国外服务，导致中国大陆用户无法访问。**必须使用国内可访问的服务或真实云存储**
2. **禁止在前端写死虚假数据**：上传和删除功能在前端写死了虚假数据，导致一直找不到图片显示不出的真正原因。**所有数据操作必须走真实API**
3. **核心原则**：前后端数据必须一致，所有功能必须通过真实API交互，禁止在前端硬编码模拟数据

### 明日计划
1. 继续优化前端性能（图片懒加载等）
2. 开始V2.0共享版开发（家庭组功能）
3. 后端同步实现家庭组相关API

## 开发状态概览
- **后端API开发**：已完成（认证系统和照片管理API已实现）
- **数据库设计**：已完成（所有数据表已创建）
- **TOS云存储集成**：已完成（照片可正常上传和显示）
- **前后端联调**：已完成
- **前端性能优化**：进行中
- **V2.0家庭组功能**：待开发

### 技术配置
- MySQL密码：112358
- MySQL端口：23306
- 后端服务端口：3000
- TOS桶名：timehouse-photos-cn-shanghai
- TOS区域：cn-shanghai（华东2）
- TOS Endpoint：tos-cn-shanghai.volces.com

---

## 2026-06-19

### 今日进展

#### 🔴 ICP 备案通过 + 端口切换
- 域名 `api.timehouse.top` ICP 备案已通过
- Nginx 移除 8443 端口，统一使用标准 443
- 阿里云安全组后续关闭 8443
- Flutter APK 重建：API_BASE_URL 改为 `https://api.timehouse.top/api/v1`

#### 🔴 应用包名修改
- 从 `com.example.timehouse` 改为 `cn.timehouse.app`
- 全国互联网安全管理服务平台 App 备案信息已整理

#### 🔴 照片去重功能
- **前端**：上传前计算 SHA-256 → 查本地 SQLite → 查服务器 MySQL → 重复则跳过
- **后端**：新增 `POST /photos/check-duplicates` 接口，photos 表添加 `hash CHAR(64)` 唯一索引
- 并发兜底：Photo.create() 捕获 ER_DUP_ENTRY 错误
- UI：重复时 SnackBar 提示"X 张照片已存在，已跳过"

#### 🟡 四个缺陷修复
| # | 问题 | 修复 |
|---|------|------|
| 1 | 昵称重新登录后丢失 | 新增 `PUT /auth/profile` API，setNickname() 调后端，smsLogin() 恢复本地昵称 |
| 2 | 成员列表不显示头像 | CircleAvatar 改为 backgroundImage + 首字母兜底 |
| 3 | 可见范围弹窗末项被遮挡 | `isScrollControlled: true` + `viewInsets.bottom` 动态避让 |
| 4 | 家庭组排序错误 | LEFT JOIN 统计照片数，按 photo_count DESC 排序 |

#### 🔵 代码托管
- 项目推送到 GitHub：`git@github.com:sunner777/-timehouse.git`
- 配置 SSH over 443 端口（国内 22 端口被封）
- .gitignore 排除 .env、build/、node_modules/

### 今日变更文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/scripts/migrate-add-hash.js` | 新增 | photos 表添加 hash 列 + 唯一索引 |
| `backend/src/models/mysql/Photo.js` | 修改 | create() 接受 hash，全部查询加 hash 列，新增 checkDuplicates/findByHash |
| `backend/src/services/photoService.js` | 修改 | uploadPhoto 透传 hash，新增 checkDuplicates 服务 |
| `backend/src/controllers/photoController.js` | 修改 | uploadPhoto 解构 hash，新增 checkDuplicates handler |
| `backend/src/routes/photo.js` | 修改 | 新增 POST /photos/check-duplicates |
| `backend/src/services/authService.js` | 修改 | 新增 updateProfile(userId, {nickname, avatar}) |
| `backend/src/controllers/authController.js` | 修改 | 新增 updateProfile handler |
| `backend/src/routes/auth.js` | 修改 | 新增 PUT /auth/profile |
| `backend/src/models/mysql/Family.js` | 修改 | findByUserId 改为 LEFT JOIN 统计照片数 |
| `backend/src/services/familyService.js` | 修改 | getFamilyList 返回 photoCount |
| `backend/nginx/timehouse.conf` | 修改 | 移除 8443 端口 |
| `timehouse/pubspec.yaml` | 修改 | 添加 crypto 依赖 |
| `timehouse/lib/src/models/photo.dart` | 修改 | 添加 String? hash 字段 |
| `timehouse/lib/src/models/family.dart` | 修改 | 添加 int photoCount 字段 |
| `timehouse/lib/src/services/api_service.dart` | 修改 | 新增 checkDuplicates()、updateProfile() |
| `timehouse/lib/src/services/storage_service.dart` | 修改 | SQLite v2 迁移，新增 checkHashLocally() |
| `timehouse/lib/src/providers/photo_provider.dart` | 修改 | 重写 uploadPhoto/uploadPhotos，哈希去重核心逻辑 |
| `timehouse/lib/src/providers/user_provider.dart` | 修改 | setNickname 调后端，smsLogin 恢复本地昵称 |
| `timehouse/lib/src/widgets/upload_fab.dart` | 修改 | 底部弹窗 isScrollControlled + 动态 padding |
| `timehouse/lib/src/screens/family_members_screen.dart` | 修改 | CircleAvatar 显示真实头像 |
| `timehouse/lib/src/screens/photos_screen.dart` | 修改 | 上传后显示重复跳过提示 |
| `timehouse/android/app/build.gradle.kts` | 修改 | namespace/applicationId → cn.timehouse.app |
| `.gitignore` | 新增 | 项目级 gitignore |

### 当前状态

| 组件 | 版本/状态 |
|------|------|
| 后端 | Node.js/Express，PM2 fork 单实例，127.0.0.1:3000 |
| 数据库 | MySQL 8.0 Docker + Redis 7 Docker，阿里云 ECS |
| 前端 | Flutter，APK 已构建 (cn.timehouse.app, 49.5MB) |
| 服务器 | 阿里云 99 计划 2C2G 3M，`121.40.161.137` |
| 域名 | `api.timehouse.top`，ICP 备案已通过，443 端口 |
| 代码仓库 | `github.com/sunner777/-timehouse` |

### 下次计划
- App 发布准备（隐私政策、用户协议、应用商店上架）
- V3.0 人脸识别技术调研
- V2.0 收尾（微信分享邀请等）

---

## 2026-06-21

### Bug 修复：换设备登录后共享组消失

#### 问题
用户 15306814275 在另一台手机上登录后，创建的共享组全部不显示。

#### 排查
- ✅ 生产数据库数据完好（用户 3 拥有 2 个共享组）
- ✅ 后端 API 正常（本地和 HTTPS 均返回正确数据）
- 🐛 **根因：前端 `families_screen.dart` 的 `build` 方法有静默错误吞没**
  - 当 `getFamilies()` API 调用失败时（网络波动、解析异常等），`_errorMessage` 被设置但 UI 完全不显示
  - UI 只判断 `isLoading` → 空状态，导致 API 异常时显示「还没有家人共享」让用户误以为数据丢失

#### 修复（commit `613fa67`）
1. **`families_screen.dart`** — 新增 `_buildErrorState()`，错误状态优先于空状态显示，展示错误消息 + 重试按钮
2. **`family_provider.dart`** — `getFamilies()` 改为逐个解析 JSON，单项失败不阻塞其余数据
3. **`my_space_screen.dart`** — 同样增加错误态处理

#### 待用户验证
- ~~需要重新构建 APK 并在另一台手机测试~~ ✅ APK 已构建
- 如果问题依然存在，错误消息会直接显示在界面上，方便进一步定位

---

### Bug 修复：昵称和头像回归（换设备后丢失 + 错误显示）

#### 问题
1. 「我的」页面昵称在换设备后丢失
2. 成员列表显示错误头像和昵称（显示 `?` 占位）

#### 排查
- 🔍 数据库 `users` 表所有用户 `nickname` 和 `avatar` 字段均为空
- 🐛 **根因：`UserProvider.setNickname()` 用了 `catch (_) {}` 静默吞掉所有 API 错误**
  - 昵称只在本地 SharedPreferences 保存，从未到达服务器
  - 换设备后本地无数据，服务器也不返回 → 空白/默认值
- 🐛 附带问题：后端 `User.update()` 总是同时 SET nickname 和 avatar，只传一个时另一个被设为 NULL

#### 修复（commit `3594d91`）
1. **`user_provider.dart`** — 重构：
   - 移除 `setNickname()` 中的静默 `catch (_) {}`
   - 新增 `_avatar` 字段 + `_restoreProfile()` 方法
   - 登录后：服务器数据优先 → 本地兜底 → 默认空
   - `setNickname()` 乐观更新 UI + 后端失败时显示错误
2. **`storage_service.dart`** — 新增 `saveAvatar()` / `getAvatar()`
3. **`profile_screen.dart`** — 显示头像图片 + 错误消息
4. **`backend/User.js`** — `update()` 改为动态构建 SET 子句，只更新传入字段

#### 部署
- 后端 `User.js` 已 rsync 到生产 + PM2 重启
- APK 已重建 (49.5MB)

---

## 2026-06-21

### 🔴 照片列表「一直转圈」Bug

#### 问题
首次进入照片列表页（我的空间→我的照片 / 共享组）一直显示转圈加载中。切换 Tab 再回来才能看到缩略图。

#### 排查过程
- ❌ 最初怀疑 API 卡住 → 实际 API 只需 13ms
- ❌ 怀疑 `addPostFrameCallback` 时序 → 改了没用
- ❌ 怀疑 `FutureBuilder` 卡住 → 去掉 FutureBuilder 还是转圈
- ✅ **最终发现：Gradle 增量编译缓存了旧 `.dex` 文件**，`flutter clean` 后代码才真正进入 APK

#### 根因
多层叠加问题：
1. `FutureBuilder` 等 future 完成 → future 卡了永远转圈
2. `isLoading` 标记 → 数据到了但 GoRouter 过渡期，`notifyListeners` 的隐式 rebuild 被丢弃
3. **Gradle 构建缓存** → 改了代码但没进 APK，前面所有测试都是白费

#### 修复（最终方案）
- `_buildPhotoTimeline`：去掉 FutureBuilder，改用同步缓存 + 后台加载
- MySpaceScreen：显式 `addListener` + `setState` 强制重建，绕开 GoRouter 隐式依赖
- 所有阻塞式 spinner 只在真正无数据时显示（`photos.isEmpty && isLoading`）
- `main()` 中预装 SQLite 缓存：第二次打开直接显示照片，零转圈

### 🔴 换设备登录后共享组消失

#### 根因
`families_screen.dart` 的 `build()` 在 API 失败时显示「还没有家人共享」空状态，错误被静默吞掉。

#### 修复
- 新增错误状态 UI（错误消息 + 重试按钮）
- `FamilyProvider.getFamilies()` 改为逐个防御性解析

### 🔴 昵称和头像回归

#### 根因
`UserProvider.setNickname()` 用 `catch (_) {}` 吞掉 API 错误，昵称只存在本地，从未到达服务器。

#### 修复
- 移除静默 catch，错误显示给用户
- 新增 avatar 字段 + `_fetchServerProfile()` 兜底
- 后端 `User.update()` 改为动态 SET（只更新传入字段）

### 🟡 App 发布准备

- ✅ 生成发布签名证书（SHA1: `DB:68:8E:2D:EB:9A:...`）
- ✅ Gradle 配置 release signing
- ✅ 隐私政策：`https://api.timehouse.top/privacy`
- ✅ 用户协议：`https://api.timehouse.top/agreement`
- ✅ 首页底部 ICP 备案号：「浙ICP备2026043339号」
- ✅ 照片预加载：启动时从 SQLite 恢复缓存，消除闪现
- 🔄 用户端待办：应用截图、软著申请、应用商店提交

### 经验教训
1. **Gradle 增量编译会缓存 `.dex`**：改 Flutter 代码后如果用户说"还是没变"，先用 `flutter clean`
2. **GoRouter `go()` 过渡期 `notifyListeners` 不可靠**：用显式 `addListener` + `setState` 兜底
3. **`FutureBuilder` 在页面过渡场景下 future 可能丢失重建信号**：优先用同步缓存 + 后台刷新
4. **`catch (_) {}` 是隐藏 bug 的温床**：至少要打日志或通知用户

---

## 2026-06-27

### 🔴 安全加固

全面的后端安全加固：

| 层面 | 内容 |
|------|------|
| 安全头 | helmet (Express) + Nginx 补充（Referrer-Policy, Permissions-Policy, Cross-Origin-Resource-Policy） |
| 输入校验 | Joi schema 覆盖 auth/photo/family 所有路由，strip unknown fields |
| 输入清理 | sanitize.js — 去控制字符、HTML 标签、bidi 覆盖字符，应用于 User.create/update、Photo.create/update |
| 配置收紧 | `express.urlencoded extended: false`、JWT 默认 secret 生产环境崩溃保护 |
| 依赖清理 | 移除 mongoose/winston/axios/uuid 四个未使用的包（减少 41 个子依赖） |
| 死代码清理 | 移除 MongoDB 配置、MongoError 处理 |

### 🔴 部署管道重构

**问题**：rsync 手动部署导致本地、服务器、GitHub 三方代码不同步——服务器有本地没有的包（@alicloud/*, puppeteer），本地有服务器没改的文件。

**解决**：
- 服务器从 `/opt/timehouse`（裸文件）改为 `/opt/timehouse-repo`（完整 git clone）
- 新增 `deploy.sh`：git pull → npm install（仅 package.json 变更时）→ PM2 reload → Nginx update
- 部署命令：`git push server master && ssh timehouse-server deploy.sh`
- 本地 SSH config 添加 `timehouse-server` 别名

**教训**：之前直接在服务器写代码后只做了服务器→GitHub 同步，忘了拉回本地，导致覆盖了服务器最新代码。已将旧代码备份恢复到 `/opt/timehouse.bak.20260627`。

### 🟡 项目文档全面更新

之前的文档长期未更新，与实际状况严重脱节（还写着火山引擎 SCF/RDS/CDN、MongoDB 等从未使用的技术）。

更新文件：
- `时光家项目规划.md` — 技术架构、V2.0 完成清单、生产环境、安全措施
- `后端技术栈选择.md` — 实际使用技术栈，去除火山引擎虚假架构
- `后端详细技术方案.md` — 依赖包、目录结构、部署架构、MongoDB 移除
- `核心记忆.md` — 新增原则12（三方同步）、原则13（每次更新文档）
- `下阶段开发计划.md` — 安全加固标记完成
- `家庭组共享功能prd.md` — 移除邀请码/短信邀请流程

### 经验教训

1. **服务器-本地-GitHub 三方必须同步**：在服务器写代码后必须拉回本地，不假设本地最新
2. **每次工作结束必须更新全部漂移文档**：只更新 CLAUDE.md 不够，技术方案/项目规划/PRD 等与实际出入较大时必须同步更新
3. **部署前先对比服务器代码**：不要假设本地和服务器一致
4. **备份优于覆盖**：任何可能覆盖的操作前先备份

### 变更文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/src/app.js` | 修改 | 添加 helmet，关闭 urlencoded extended |
| `backend/src/config/index.js` | 修改 | 移除 MongoDB 配置，JWT 生产环境保护 |
| `backend/src/middleware/errorHandler.js` | 修改 | 移除 MongoError 处理 |
| `backend/src/middleware/validate.js` | 新增 | Joi 通用校验中间件 |
| `backend/src/validators/schemas.js` | 新增 | auth/photo/family 全量 Joi schema |
| `backend/src/utils/sanitize.js` | 新增 | 文本清理（控制字符/HTML/bidi） |
| `backend/src/models/mysql/User.js` | 修改 | nickname 经过 sanitize |
| `backend/src/models/mysql/Photo.js` | 修改 | location/tags 经过 sanitize |
| `backend/src/routes/auth.js` | 修改 | 所有路由挂载 validate 中间件 |
| `backend/src/routes/photo.js` | 修改 | 所有路由挂载 validate 中间件 |
| `backend/src/routes/family.js` | 修改 | create/addMember/updatePermission 挂载 validate |
| `backend/nginx/timehouse.conf` | 修改 | 新增三个安全头，修复 limit_req_zone 位置 |
| `backend/package.json` | 修改 | 移除 mongoose/winston/axios/uuid，添加 helmet，补回 @alicloud/* 和 puppeteer |
| `backend/deploy.sh` | 重写 | rsync 版 → git-pull 版 |
| `*.md` | 修改 | 6 个项目文档全面更新 |

---

### 🟡 死代码清理 + Bug 修复

基于服务器最新代码全面审计后修复：

| # | 问题 | 修复 |
|---|------|------|
| 1 | Flutter `joinFamily()` / `generateInviteCode()` 调用不存在的后端接口 | 从 `api_service.dart` 和 `family_provider.dart` 移除 |
| 2 | `photos_screen.dart` 不被路由引用，V1.0 残留 | 删除 |
| 3 | `src/models/mongodb/`, `src/queues/`, `src/workers/`, `tests/` 空目录 | 清理 6 个空目录 |
| 4 | auth 事件（register/login/smsLogin/logout）无审计日志 | 补充 `auditLog()` 调用 |
| 5 | `photos.hash` 缺唯一索引（去重靠 SELECT 查询，索引为并发兜底） | 创建 `migrate-add-hash-index.js`，生产执行 |

### 🟢 优化

| 项目 | 变更 |
|------|------|
| PM2 | `fork` 单进程 → `cluster` 2 实例 |
| `deploy.sh` | rsync 版 → git-pull 版（与部署管道一致） |
| Gradle 自动签名 | 修复（signing config 名 `myRelease` → `release`），以后 `flutter build apk` 直接产出签名包 |

### 变更文件（第二阶段）

| 文件 | 操作 | 说明 |
|------|------|------|
| `timehouse/lib/src/services/api_service.dart` | 修改 | 移除 joinFamily / generateInviteCode |
| `timehouse/lib/src/providers/family_provider.dart` | 修改 | 移除 joinFamily / generateInviteCode |
| `timehouse/lib/src/screens/photos_screen.dart` | 删除 | V1.0 死代码 |
| `timehouse/android/app/build.gradle.kts` | 修改 | signing config 改名 + 去调试代码 |
| `backend/src/services/authService.js` | 修改 | register/login/smsLogin 加 auditLog |
| `backend/src/controllers/authController.js` | 修改 | logout 加 auditLog |
| `backend/scripts/migrate-add-hash-index.js` | 新增 | hash 唯一索引迁移脚本 |
| `backend/ecosystem.config.js` | 修改 | cluster 2 实例 |
| `backend/deploy.sh` | 修改 | git-pull 版 |

### 三端同步确认

| 端 | 提交 | 状态 |
|------|------|------|
| 本地 | `ddf5c7c` | ✅ |
| GitHub | `ddf5c7c` | ✅ |
| 服务器 | `ddf5c7c` | ✅ |
| 服务器运行 | PM2 cluster 2 实例，健康检查通过 | ✅ |
